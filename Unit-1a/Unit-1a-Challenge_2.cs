using System;

namespace MyApplication
{
  class Program
  {
    static void Main(string[] args)
    {
      int x = 5;
      int y = 3;
      Console.WriteLine(x >= y); // returns False because 5 is neither less than or equal to 3. With this one, I learned it doesn't matter the number, it matters what side of the = you put the </> sign (whichever you're using).
     }
  }
}